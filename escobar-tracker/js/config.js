// 1) Paste your Firebase web app config here (from Firebase Console)
export const firebaseConfig = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME",
  projectId: "PASTE_ME",
  appId: "PASTE_ME",
};

// 2) Simple shared code (not encrypted on purpose — this is just a “gate”)
export const OFFICER_PORTAL_CODE = "1234"; // change this

export const APP_TZ = "Asia/Manila";
